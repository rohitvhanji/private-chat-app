{
  "name": "private-chat-app",
  "version": "1.0.0",
  "description": "Private chat application for two users",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm run build",
    "deploy": "gh-pages -d client/build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
